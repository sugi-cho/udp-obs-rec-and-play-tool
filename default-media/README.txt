Put bundled test media files in this folder for packaged app auto-load.

Required:
- one .mp4 file
- one .jsonl (or .json extension with JSONL content) UDP log file

Example:
- sample.mp4
- sample.jsonl
